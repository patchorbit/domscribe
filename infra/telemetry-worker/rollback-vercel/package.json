{
  "name": "@domscribe/telemetry-vercel-fallback",
  "version": "0.0.0",
  "private": true,
  "description": "Vercel Edge Function rollback target for the Cloudflare telemetry Worker (RFC 0002 §4 replanning trigger #2).",
  "type": "module",
  "scripts": {
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^5.6.0",
    "vercel": "^48.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
