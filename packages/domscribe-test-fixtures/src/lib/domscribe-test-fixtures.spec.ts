import { domscribeTestFixtures } from './domscribe-test-fixtures.js';

describe('domscribeTestFixtures', () => {
  it('should work', () => {
    expect(domscribeTestFixtures()).toEqual('domscribe-test-fixtures');
  });
});
